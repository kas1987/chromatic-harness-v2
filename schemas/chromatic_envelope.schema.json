{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Chromatic Event Envelope",
  "description": "Discriminated union wrapper for all harness event types. 'kind' selects the payload schema; full payload validation is performed by envelope_validator.py.",
  "type": "object",
  "required": ["kind", "payload"],
  "properties": {
    "kind": {
      "type": "string",
      "enum": ["harness_event", "magnet_event", "bead"],
      "description": "Discriminator key that identifies which schema governs 'payload'"
    },
    "payload": {
      "type": "object",
      "description": "The event payload; schema is determined by 'kind'"
    }
  },
  "additionalProperties": false
}
