# v2.1 Upgrade Notes

v2.1 adds the missing operational controls from the v2 audit:

- File claim/release locking
- Command wrapper with automatic failure logging
- Event router to incident/collision/queue artifacts
- Stronger schema validation
- Git state snapshots
- Secret scanner
- Report generation
- VS Code tasks
- Git hook templates
- GitHub Actions validation
- Clean package manifest and `.gitignore`
